1. GC算法待优化，目前是从待检查节点出发查找根节点，未对节点染色，缓存等，导致公共父节点会被重复判断
2. 装饰器Decorator未实现
3. 断点Breakpoint未实现
4. 对象存储结构不合理，统一Variable key导致大量不必要的Variable生命周期管理，应改为字符串+Symbol的方式存储
5. 数组存储结构不合理，单纯借用对象结构，操作过程中存在较多数字-字符串转换
6. HashMap结构原始，桶结构浪费内存，应修改为红黑树实现
7. 正则依赖至PCRE2库，国际化依赖自ICU库，应尝试消减
8. 精准报告调用栈未实现，LOCATION指令已加入